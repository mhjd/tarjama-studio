`preview.mp4` is a synthetic three-second 320×480 colour panel with a 440 Hz
tone and Arabic subtitles, generated by `TestRealMediaAndExports`. No user media
or voice is included. It is used only by the explicit `preview_live` test build.
